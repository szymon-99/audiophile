import React, { FC } from "react"
import styled from "styled-components"

const Footer: FC = () => {
  return <Wrapper>Fooooter</Wrapper>
}

const Wrapper = styled.footer`
  background-color: var(--black);
  color: var(--white);
`

export default Footer
